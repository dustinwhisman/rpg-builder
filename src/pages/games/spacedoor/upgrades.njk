---
title: Upgrades
game: Spacedoor!
description: See all the available upgrades that you can choose from.
---

{% extends "layouts/default.njk" %}

{% block content %}
<article class="cmp-container cmp-stack">
  <h1>
    Upgrades
  </h1>
  <p>
    Trade in your Experience Points to buy upgrades for your character.
  </p>

  <h2>
    Stat Upgrades
  </h2>
  {% for stat, skills in stats %}
    <h3>
      {{ stat }}
    </h3>
    {% for upgrade in statUpgrades %}
      {% if stat == upgrade.stat %}
        <article class="cmp-stack">
          <h4>{{ upgrade.name }}</h4>
          <p>
            {{ upgrade.description }}
          </p>
          <p>
            Cost: {{ upgrade.cost }} XP
          </p>
        </article>
      {% endif %}
    {% endfor %}
  {% endfor %}

  <h2>
    Skill Upgrades
  </h2>
  {% for stat, skills in stats %}
    {% for skill in skills %}
      <h3>
        {{ skill }}
      </h3>
      {% for upgrade in skillUpgrades %}
        {% if skill == upgrade.skill %}
          <article class="cmp-stack">
            <h4>{{ upgrade.name }}</h4>
            <p>
              {{ upgrade.description }}
            </p>
            <p>
              Cost: {{ upgrade.cost }} XP
            </p>
          </article>
        {% endif %}
      {% endfor %}
    {% endfor %}
  {% endfor %}
</article>
{% endblock %}
