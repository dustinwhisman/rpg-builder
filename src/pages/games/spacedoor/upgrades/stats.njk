---
title: Stat Upgrades
game: Spacedoor!
description: See all the stat upgrades that you can choose from.
---

{% extends "layouts/default.njk" %}

{% block content %}
<article class="cmp-container cmp-stack">
  <h1>
    Stat Upgrades
  </h1>
  <p>
    Upgrade stat dice or add permanent bonuses to core stat rolls.
  </p>

  {% for stat, skills in stats %}
    <h2>
      {{ stat }}
    </h2>
    <div class="cmp-upgrade-card__grid">
      {% for upgrade in statUpgrades %}
        {% if stat == upgrade.stat %}
          <article class="cmp-upgrade-card">
            <h3 class="cmp-upgrade-card__name">
              {{ upgrade.name }}
            </h3>
            <p class="cmp-upgrade-card__description">
              {{ upgrade.description }}
            </p>
            <p class="cmp-upgrade-card__cost">
              <span class="cmp-upgrade-card__cost-label">
                Cost:
              </span>
              <strong>
                {{ upgrade.cost }} XP
              </strong>
            </p>
          </article>
        {% endif %}
      {% endfor %}
    </div>
  {% endfor %}
</article>
{% endblock %}
